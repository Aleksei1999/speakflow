// Каталог преподавателей для учителя — re-export из /student/teachers
// (та же сетка карточек, фильтры, модалка). readOnly режим автоматически
// активируется для не-студентов (role !== 'student' && role !== null).
// Учитель не может бронировать у других преподов и оставлять отзывы.
export { default } from "../../student/teachers/page"
