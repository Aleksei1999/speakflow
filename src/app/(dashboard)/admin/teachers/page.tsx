// Каталог преподавателей для админа — re-export из /student/teachers
// (та же сетка карточек, фильтры по специализации/цене/рейтингу,
// модалка с био+слотами+отзывами). Идентичный UX, без модерации.
export { default } from "../../student/teachers/page"
